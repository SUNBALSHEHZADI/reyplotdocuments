
# **Axis Limits**

By default, ReyPlot computes axis limits based on your data, adding a 5% padding. You can override these with `x_lim()` and `y_lim()`.

## Set X‑Axis Limits

```python
fig.x_lim([min_value, max_value])
```
